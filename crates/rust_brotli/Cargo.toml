[package]
name = "rust_brotli"
version = "0.1.0"
edition = "2021"

[dependencies]
fuzz_utils = { path = "../fuzz_utils" }
brotli = { git = "https://github.com/dropbox/rust-brotli.git" }

[profile.release]
overflow-checks = true
panic = "abort"
debug = true
debug-assertions = true
