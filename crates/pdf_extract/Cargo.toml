[package]
name = "pdf_extract"
version = "0.1.0"
edition = "2021"

[dependencies]
fuzz_utils = { path = "../fuzz_utils" }
pdf-extract = {git = "https://github.com/jrmuizel/pdf-extract"}

[profile.release]
overflow-checks = true
panic = "abort"
debug = true
debug-assertions = true