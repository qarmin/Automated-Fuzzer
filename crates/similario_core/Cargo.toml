[package]
name = "similario_core"
version = "0.1.0"
edition = "2021"

[dependencies]
fuzz_utils = { path = "../fuzz_utils" }
similario_core = { git = "https://github.com/qarmin/similario.git" }

[profile.release]
overflow-checks = true
panic = "abort"
debug = true
debug-assertions = true
