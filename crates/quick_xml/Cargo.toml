[package]
name = "quick_xml"
version = "0.1.0"
edition = "2021"

[dependencies]
fuzz_utils = { path = "../fuzz_utils" }
quick-xml = { git = "https://github.com/tafia/quick-xml.git" }

[profile.release]
overflow-checks = true
panic = "abort"
debug = true
debug-assertions = true
