[package]
name = "infer"
version = "0.1.0"
edition = "2021"

[dependencies]
fuzz_utils = { path = "../fuzz_utils" }
infer = { git = "https://github.com/bojand/infer.git" }

[profile.release]
overflow-checks = true
panic = "abort"
debug = true
debug-assertions = true
