[package]
name = "swash"
version = "0.1.0"
edition = "2021"

[dependencies]
fuzz_utils = { path = "../fuzz_utils" }
swash = { git = "https://github.com/dfrg/swash.git" }

[profile.release]
overflow-checks = true
panic = "abort"
debug = true
debug-assertions = true
